#include <stdio.h>
int main()
{
    int a = 5;
    printf(" %d ", a); // printf prints everything in double quotes as it is including the spaces and it swaps the format specifiers
    return 0;                // like %d with its argument values here a
}