/* for loop has 3 parts, each of which is optional
 * for ( initialization; test; update)
 */

#include<stdio.h>
int main()
{
    int i = 0;
    for ( ; i<=9; )
    {
        i++;
        printf("\n vaue is: %d", i);
    }

    return 0;
}