/* Conditional Statements
    i) the if stmt
    ii) the switch stmt

    if (test expression / condition)
    {
      statement-block;  // if condition is true then we will execute the stmt-block
     }
 */

#include<stdio.h>
int main()
{
    int target = 10;
    if (target == 10)
    {
        printf("Target is equal to 10");
    }

    return 0;
}